//(reverse rotate a) : Décale d’une position vers le bas tous les élements de
// la pile a. Le dernier élément devient le premier.
#include "push_swap.h"

void rra(stack_list **stack_A, stack_list **stack_B)
{
	printf("rra");
}